'use strict';

// Register `phoneList` component, along with its associated controller and template
angular.
  module('auth').
  component('auth', {
    templateUrl: 'auth/auth.template.html',
    controller: ['$http','$window','$rootScope', function authController($http,$window,$rootScope) {
      
        
    }]
  });